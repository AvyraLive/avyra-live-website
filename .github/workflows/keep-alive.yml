# Ping le site toutes les 14 minutes pour empêcher Render Free
# de mettre l'instance en veille (spin down après 15 min d'inactivité)
name: Keep Alive

on:
  schedule:
    # Toutes les 14 minutes, 24h/24, 7j/7
    - cron: '*/14 * * * *'
  workflow_dispatch: # Permet de lancer manuellement

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping avyra-live.fr
        run: |
          curl -sf --max-time 30 https://avyra-live.fr > /dev/null
          echo "✅ Site pingé avec succès à $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
